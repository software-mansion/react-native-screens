package com.swmansion.rnscreens.stack.animation.spec

/**
 * Where a disappearing screen is drawn relative to the other screen of the transition.
 */
internal enum class ZPolicy {
    UNDER,
    OVER,
}
