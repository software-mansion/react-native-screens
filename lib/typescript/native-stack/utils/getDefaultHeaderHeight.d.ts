import { StackPresentationTypes } from 'react-native-screens';
declare type Layout = {
    width: number;
    height: number;
};
export default function getDefaultHeaderHeight(layout: Layout, statusBarHeight: number, stackPresentation: StackPresentationTypes): number;
export {};
