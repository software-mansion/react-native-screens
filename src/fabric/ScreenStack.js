import * as React from 'react';
import ScreenStackNativeComponent from './ScreenStackNativeComponent';

const ScreenStack = function (props) {
  return <ScreenStackNativeComponent {...props} />;
};

export default ScreenStack;
