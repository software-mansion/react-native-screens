name: Test iOS e2e - new architecture
on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/ios-e2e-test-fabric.yml'
      - '.github/actions/argent-server/**'
      - '.argent/**'
      - 'RNScreens.podspec'
      - 'package.json'
      - 'ios/**'
      - 'common/**'
      - 'FabricExample/**'
      - 'apps/**'
      - 'src/**'
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      argent-only:
        description: Skip the Detox tests; only build the app and run the Argent flows.
        type: boolean
        default: false
jobs:
  test:
    runs-on: macos-26-xlarge
    timeout-minutes: 90
    env:
      WORKING_DIRECTORY: FabricExample
    concurrency:
      group: ios-e2e-fabric-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Setup Node.js (version from .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'
      - name: Use latest stable Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.2'
      - name: Get Xcode version
        run: xcodebuild -version
      - name: Install AppleSimulatorUtils
        run: brew tap wix/brew && brew trust wix/brew && brew install applesimutils
      - name: Install root node dependencies
        run: yarn install && yarn submodules
      - name: Install node dependencies
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: yarn
        # This step is required due to the bug introduced in 1.15.0 version of cocoapods
        # that breaks the pod installation for cached packages.
        # see https://github.com/facebook/react-native/issues/42698
        # This step should be removed once this issue will be resolved:
        # https://github.com/actions/runner-images/issues/9308
      - name: Install Ruby gems
        run: gem install cocoapods -v 1.15.2
      - name: Install pods
        working-directory: ${{ env.WORKING_DIRECTORY }}/ios
        run: pod install
      - name: Build app
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: yarn build-e2e-ios
      # Hands the release build to the `argent` job (jobs run on separate
      # machines). Internal only — nothing to download from the run page.
      - name: Package app for Argent
        run: |
          tar -czf "$RUNNER_TEMP/FabricExample-app.tar.gz" \
            -C "$WORKING_DIRECTORY/ios/build/Build/Products/Release-iphonesimulator" FabricExample.app
      - name: Upload app for Argent
        uses: actions/upload-artifact@v4
        with:
          name: build-ios-app
          path: ${{ runner.temp }}/FabricExample-app.tar.gz
          retention-days: 1
      - name: Test app
        if: ${{ !inputs.argent-only }}
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: yarn test-e2e-ios
      - uses: actions/upload-artifact@v4
        if: ${{ failure() }}
        with:
          name: ios-fail-screen-shots
          path: ${{ env.WORKING_DIRECTORY }}/artifacts

  argent:
    # Separate job so the Argent flows get their own time budget and a fresh
    # simulator, whatever state the Detox run left behind. Runs even when Detox
    # failed, as long as the app artifact was uploaded.
    needs: test
    if: ${{ !cancelled() }}
    runs-on: macos-26-xlarge
    timeout-minutes: 30
    concurrency:
      group: ios-argent-fabric-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: checkout
        uses: actions/checkout@v4
      # Only needed to run the Argent CLI — the app is downloaded, not built.
      - name: Setup Node.js (version from .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
      # Same Xcode as the build, so the simulator runtime matches the SDK.
      - name: Use latest stable Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.2'
      - name: Download app built by the test job
        uses: actions/download-artifact@v4
        with:
          name: build-ios-app
          path: ${{ runner.temp }}/app
      - name: Unpack app
        run: |
          tar -xzf "$RUNNER_TEMP/app/FabricExample-app.tar.gz" -C "$RUNNER_TEMP/app"
          echo "APP_PATH=$RUNNER_TEMP/app/FabricExample.app" >> "$GITHUB_ENV"
      # iPhone 17 matches the geometry of the committed iOS baselines (1206x2622).
      # A freshly created simulator, never one another job has used.
      - name: Boot iPhone 17 simulator
        run: |
          RUNTIME=$(xcrun simctl list runtimes ios -j | jq -r '.runtimes | map(select(.isAvailable)) | last | .identifier')
          UDID=$(xcrun simctl create "Argent iPhone 17" com.apple.CoreSimulator.SimDeviceType.iPhone-17 "$RUNTIME")
          echo "Booting $UDID ($RUNTIME)"
          xcrun simctl boot "$UDID"
          xcrun simctl bootstatus "$UDID"
          echo "SIMULATOR_UDID=$UDID" >> "$GITHUB_ENV"
      - name: Install app on simulator
        run: xcrun simctl install "$SIMULATOR_UDID" "$APP_PATH"
      - name: Install Argent and start the tool-server
        uses: ./.github/actions/argent-server
      # --output keeps baseline/current/diff PNGs of every mismatching snapshot.
      - name: Run Argent flows
        run: |
          set -o pipefail
          mkdir -p "$RUNNER_TEMP/argent-out"
          argent flow run ./.argent/flows --device "$SIMULATOR_UDID" \
            --output "$RUNNER_TEMP/argent-out/snapshots" 2>&1 | tee "$RUNNER_TEMP/argent-out/argent-flows.log"
      - name: Capture screen at failure
        if: ${{ failure() }}
        run: |
          mkdir -p "$RUNNER_TEMP/argent-out"
          xcrun simctl io "$SIMULATOR_UDID" screenshot "$RUNNER_TEMP/argent-out/screen-at-failure.png" || true
      - name: Summarize Argent failure
        if: ${{ failure() }}
        run: |
          cp "${ARGENT_SERVER_LOG:-/nonexistent}" "$RUNNER_TEMP/argent-out/" 2>/dev/null || true
          scripts/e2e/argent-failure-summary.sh ios "$RUNNER_TEMP/argent-out"
      - name: Upload Argent failure artifact
        if: ${{ failure() }}
        uses: actions/upload-artifact@v4
        with:
          name: argent-ios-failure
          path: ${{ runner.temp }}/argent-out
          if-no-files-found: ignore
